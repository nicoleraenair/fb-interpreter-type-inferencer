open Fbast;;

exception TypeMismatch;;

(** Evaluates the provided expression. *)
let rec eval (e : expr) : expr =
  (* NOTE: You may replace this body with an F♭ evaluation function, but you
     are not required to do so. *)
  e
;;
